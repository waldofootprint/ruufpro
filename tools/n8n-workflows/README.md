# n8n Workflow Templates

Reference workflows from AI Automation by Jack (Skool). Import into n8n if/when we adopt it.

## Files
- `scraperrrr.json` — Full pipeline: Apify lead scraping → AI research → personalized email + LinkedIn → Google Sheets
- `scraper_google_maps.json` — Google Maps scraping → website analysis → AI summary → Google Sheets

## Status
These are reference files only. RuufPro currently uses Python scripts (`find_prospects.py`, `enrich_prospects.py`, `generate_email_sequence.py`) for the same pipeline. See vault entry 070 for analysis.
